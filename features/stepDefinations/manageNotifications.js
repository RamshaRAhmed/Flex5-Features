const { Given, When, Then } = require('@cucumber/cucumber');

When('the admin creates and sends a notification to a user', function () {
    // Code to create and send a notification to a user
});

Then('the notification should be successfully delivered to the user', function () {
    // Code to verify that the notification is successfully delivered to the user
});
